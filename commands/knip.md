Run `pnpm run knip` in this project. Based on the output analysis of unused code, plan the cleanup this project to prune from unused code

if you find `knip` is missing, run `pnpm create @knip/config` and then you can run the command above. 